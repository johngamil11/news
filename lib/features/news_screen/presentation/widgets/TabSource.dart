import 'package:flutter/material.dart';

class TabSource extends StatelessWidget {
  const TabSource
({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}